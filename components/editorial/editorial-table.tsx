import type { EditorialTable as Table } from "@/types/editorial-content"
export function EditorialTable({ table }: { readonly table: Table }) { return <div className="max-w-full overflow-x-auto rounded-xl border"><table className="w-full min-w-xl border-collapse text-left text-sm"><caption className="border-b bg-muted/40 p-4 text-left font-semibold">{table.caption}</caption><thead><tr>{table.headers.map((header) => <th key={header} scope="col" className="border-b p-4 font-semibold">{header}</th>)}</tr></thead><tbody>{table.rows.map((row, index) => <tr key={`${row[0]}-${index}`} className="border-b last:border-0">{row.map((cell, cellIndex) => cellIndex === 0 ? <th key={cellIndex} scope="row" className="p-4 font-medium">{cell}</th> : <td key={cellIndex} className="p-4 text-muted-foreground">{cell}</td>)}</tr>)}</tbody></table></div> }
