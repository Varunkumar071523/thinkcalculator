import{describe,expect,it}from"vitest";import{calculateFDSchedule}from"../calculate-fd-schedule";import{calculateFD}from"../calculate-fd";import type{FDInput}from"../fd-types";const standard:FDInput={principalAmount:100000,annualInterestRate:7,duration:5,durationUnit:"years",compoundingFrequency:"quarterly"};describe("FD schedule",()=>{it("reconciles quarterly standard",()=>expect(calculateFDSchedule(standard).at(-1)?.maturityAmount).toBeCloseTo(calculateFD(standard).maturityAmount,8));it.each(["monthly","yearly"] as const)("supports %s compounding",compoundingFrequency=>expect(calculateFDSchedule({...standard,compoundingFrequency}).at(-1)?.maturityAmount).toBeCloseTo(calculateFD({...standard,compoundingFrequency}).maturityAmount,8));it("handles zero interest and months",()=>{const rows=calculateFDSchedule({...standard,annualInterestRate:0,duration:6,durationUnit:"months"});expect(rows.every(r=>r.interestEarned===0)).toBe(true)});it("rejects invalid",()=>expect(()=>calculateFDSchedule({...standard,principalAmount:-1})).toThrow(RangeError))})
