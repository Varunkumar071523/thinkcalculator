import{describe,expect,it}from"vitest";import{calculateLumpsumSchedule}from"../calculate-lumpsum-schedule";import{calculateLumpsum}from"../calculate-lumpsum";import type{LumpsumInput}from"../lumpsum-types";const standard:LumpsumInput={initialInvestment:100000,annualReturnRate:12,duration:10,durationUnit:"years"};describe("Lumpsum schedule",()=>{it("creates standard rows and reconciles",()=>{const rows=calculateLumpsumSchedule(standard);expect(rows).toHaveLength(10);expect(rows.at(-1)?.futureValue).toBeCloseTo(calculateLumpsum(standard).futureValue,8)});it("handles zero return and months",()=>{const rows=calculateLumpsumSchedule({...standard,annualReturnRate:0,duration:6,durationUnit:"months"});expect(rows).toHaveLength(6);expect(rows.every(r=>r.estimatedReturns===0)).toBe(true)});it("returns never decrease",()=>{const rows=calculateLumpsumSchedule(standard);expect(rows.every((r,i)=>i===0||r.estimatedReturns>=rows[i-1].estimatedReturns)).toBe(true)});it("rejects invalid",()=>expect(()=>calculateLumpsumSchedule({...standard,initialInvestment:-1})).toThrow(RangeError))})
