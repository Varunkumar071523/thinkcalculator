import { calculateRD } from "@/features/calculators/rd/calculate-rd"
import type { RDInput, RDScheduleRow } from "@/features/calculators/rd/rd-types"
function rowMonths(totalMonths:number){const step=totalMonths>24?12:1;const points:number[]=[];for(let month=step;month<totalMonths;month+=step)points.push(month);points.push(totalMonths);return points}
export function calculateRDSchedule(input:RDInput):readonly RDScheduleRow[]{const result=calculateRD(input);const rate=input.annualInterestRate/100;return rowMonths(result.totalMonths).map((monthsElapsed,index)=>{const totalDeposited=input.monthlyDeposit*monthsElapsed;let maturityAmount=totalDeposited;if(rate!==0){maturityAmount=0;for(let contributionMonth=0;contributionMonth<monthsElapsed;contributionMonth+=1){const remainingYears=(monthsElapsed-contributionMonth)/12;maturityAmount+=input.monthlyDeposit*Math.pow(1+rate/result.compoundingPeriodsPerYear,result.compoundingPeriodsPerYear*remainingYears)}}return{periodNumber:index+1,monthsElapsed,totalDeposited,interestEarned:maturityAmount-totalDeposited,maturityAmount}})}
