import{describe,expect,it}from"vitest";import{calculateRDSchedule}from"../calculate-rd-schedule";import{calculateRD}from"../calculate-rd";import type{RDInput}from"../rd-types";const standard:RDInput={monthlyDeposit:5000,annualInterestRate:7,duration:5,durationUnit:"years",compoundingFrequency:"quarterly"};describe("RD schedule",()=>{it("reconciles standard maturity and deposits",()=>{const row=calculateRDSchedule(standard).at(-1),result=calculateRD(standard);expect(row?.maturityAmount).toBeCloseTo(result.maturityAmount,8);expect(row?.totalDeposited).toBe(result.totalDeposited)});it("handles zero interest and months",()=>{const rows=calculateRDSchedule({...standard,annualInterestRate:0,duration:12,durationUnit:"months"});expect(rows.every(r=>r.interestEarned===0)).toBe(true)});it("deposits never decrease",()=>{const rows=calculateRDSchedule(standard);expect(rows.every((r,i)=>i===0||r.totalDeposited>=rows[i-1].totalDeposited)).toBe(true)});it("rejects invalid",()=>expect(()=>calculateRDSchedule({...standard,monthlyDeposit:-1})).toThrow(RangeError))})
